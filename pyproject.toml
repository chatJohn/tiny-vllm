[project]
name = "tiny-vllm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flash-attn==2.5.8",
    "numpy>=2.4.4",
    "psutil>=7.2.2",
    "torch>=2.11.0",
    "triton>=3.6.0",
]
